// Deprecated — no longer imported anywhere. This wrapped the Lovable editor's
// in-browser error bridge (window.__lovableEvents), which is not present
// outside that environment. Kept only until a delete pass removes the file.
export {};
